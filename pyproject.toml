
[tool.uv]
package = true

[project.scripts]
vd-games = "VD_games.scripts.VD_games:main"
[project]
name = "vd-games"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prompt>=0.4.1",
]
